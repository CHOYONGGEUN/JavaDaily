package javaplus.accessmodifier.Ex_2;

public class AccessModifierOfClass_1 {
    public static void main(String[] args) {

        //1. 객체 생성
        A a = new A();//객체 생성 가능(A a ) + 생성자 호출 가능 (new A())
        B b = new B();//객체 생성 가능(A a ) + 생성자 호출 가능 (new A())
        C c = new C();//객체 생성 가능(A a ) + 생성자 호출 가능 (new A())
        D d = new D();//객체 생성 가능(A a ) + 생성자 호출 가능 (new A())
    }
}
